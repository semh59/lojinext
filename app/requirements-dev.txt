# Development & test dependencies
# These are NOT installed in the production Docker image.
# Install with: pip install -r app/requirements-dev.txt

# ── Testing ───────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-mock>=3.14
pytest-timeout>=2.3
# Paralel unit test koşumu — CI'da ~%60 hızlanma. Integration testlere
# uygulanmadı (DB session race riski).
pytest-xdist>=3.6
respx>=0.21
httpx>=0.27          # async HTTP client used in tests
faker>=25.0          # realistic fixture data generation
anyio>=4.0           # async test runtime

# ── Linting & formatting ──────────────────────────────────────
ruff>=0.4            # fast Python linter + formatter
black>=24.0          # code formatter (optional, ruff can replace)

# ── Architecture boundary enforcement (modular monolith, FAZ0+) ─
import-linter==2.13  # static import-graph contracts, report-only until FAZ1 gate

# ── Type checking ─────────────────────────────────────────────
mypy>=1.10
# PyJWT ships its own type hints (py.typed); python-jose stubs removed with the
# jose -> PyJWT migration (MINOR-009).
types-passlib
# arch/fk_registry.yml (FAZ2 DB rol izolasyonu Wave 1) parsing —
# app/tests/integration/test_fk_registry_matches_live_schema.py +
# scripts/faz2_generate_fk_registry_seed.py
PyYAML>=6.0
types-PyYAML

# ── Security scanning ─────────────────────────────────────────
pip-audit>=2.7       # CVE scanner for installed packages

# ── Misc dev helpers ─────────────────────────────────────────
ipython>=8.0
pre-commit>=3.7
