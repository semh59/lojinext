# Faz 10 — Güzergah Simülasyon UI Implementation Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans. Checkbox adımları.

**Goal:** Koordinat/lokasyon+yük+araç → `/routes/simulate` → recharts profil +
SVG heatmap + özet gösteren Rota Lab sayfası. Spec: `2026-06-14-faz10-rota-lab-design.md`.

**Tech Stack:** FastAPI (mevcut endpoint), React/TS, React Query, recharts, vitest.

---

### Task 1: Backend — response'a koordinat + hız annotation

**Files:** `app/api/v1/endpoints/routes.py`; Test `app/tests/integration/test_routes_simulate.py` (varsa ek, yoksa unit serialize testi).

- [ ] **Step 1:** `SegmentSimResponse`'a alan ekle: `mid_lon: Optional[float] = None`, `mid_lat: Optional[float] = None`, `maxspeed_kmh: Optional[float] = None`, `traffic_speed_kmh: Optional[float] = None`, `congestion: str = "low"`, `speed_source: str = "road_class"`.
- [ ] **Step 2:** `_serialize_simulation`'da `SegmentSimResponse(...)`'a doldur: `mid_lon=s.mid_lon, mid_lat=s.mid_lat, maxspeed_kmh=s.maxspeed_kmh, traffic_speed_kmh=s.traffic_speed_kmh, congestion=s.congestion or "low"`. (speed_source RouteSegment'te yok → türet: traffic>0?"traffic":maxspeed>0?"maxspeed":"road_class".)
- [ ] **Step 3:** routes.py persist `RouteSegment(...)` (line ~272) build'ine `maxspeed_kmh=s.maxspeed_kmh, traffic_speed_kmh=s.traffic_speed_kmh, congestion=s.congestion` ekle (s artık SegmentOutput, Task 6'dan taşıyor). Eski "kayboluyor" yorumunu sil.
- [ ] **Step 4:** Test — `_serialize_simulation` mid_lon/lat + speed_source döndürür (unit, ORM stub). Run: `bash -c "cd /app && python -m pytest app/tests/ -k 'route' -q"`.
- [ ] **Step 5:** ruff+mypy; Commit: `feat(routes): simulate response'a segment koordinat + hız annotation`

---

### Task 2: Frontend service + hook + tipler

**Files:** Create `frontend/src/services/api/route-sim-service.ts`, `frontend/src/hooks/useRouteSimulation.ts`; Test `__tests__/`.

- [ ] **Step 1: Failing test** — `route-sim-service.test.ts`: mock axiosInstance, `simulateRoute({lokasyon_id:1,ton:20})` → POST `/routes/simulate` body, response tipi.
- [ ] **Step 2:** `route-sim-service.ts` — `axiosInstance.post('/routes/simulate', req)`; export tipler `RouteSimRequest` (lokasyon_id?|cikis/varis lat-lon?, ton, arac_yasi, segment_length_m), `SegmentSim` (seq,length_km,grade_pct,road_class,sim_speed_kmh,sim_l_per_100km,sim_l_total,eta_sec,mid_lon,mid_lat,maxspeed_kmh,traffic_speed_kmh,congestion,speed_source), `RouteSimResponse` (simulation_id,summary,segments,elevation_coverage_pct,meta).
- [ ] **Step 3:** `useRouteSimulation.ts` — React Query `useMutation(simulateRoute)`.
- [ ] **Step 4:** Run service+hook testleri → pass. `npx vitest --run src/services/api/__tests__/route-sim-service.test.ts`.
- [ ] **Step 5:** Commit: `feat(route-lab): route-sim service + useRouteSimulation hook`

---

### Task 3: Görselleştirme bileşenleri

**Files:** Create `frontend/src/features/route-lab/RouteHeatmap.tsx`, `RouteProfileChart.tsx`, `RouteSimSummary.tsx`; `resources/tr/routeLab.ts`; Test `__tests__/`.

- [ ] **Step 1: routeLab.ts** — TR string'ler (başlık, form etiketleri, kartlar, eşik açıklamaları, hata mesajları).
- [ ] **Step 2: RouteSimSummary** — kartlar (mesafe/süre/toplam L/avg/ascent/descent/coverage). Test: değerler render.
- [ ] **Step 3: RouteProfileChart** — recharts ComposedChart; segment[] → kümülatif mesafe; Line sim_speed_kmh, Line sim_l_per_100km, Area grade_pct. Test: render + segment sayısı.
- [ ] **Step 4: RouteHeatmap** — SVG: mid_lon/lat → equirectangular `x=(lon-minLon)/(maxLon-minLon)*W`, `y=(maxLat-lat)/(maxLat-minLat)*H` (lat ters); ardışık segment polyline `stroke=colorFor(sim_l_per_100km)` (<30 yeşil #22c55e, <40 kehribar #f59e0b, else kırmızı #ef4444); hover→tooltip. `colorForConsumption(l)` saf fonksiyon export. Test: eşik renkleri + projeksiyon (min/max → 0/1).
- [ ] **Step 5:** ruff yok (FE); `npm run lint`; Commit: `feat(route-lab): heatmap + profil + özet bileşenleri`

---

### Task 4: Form + sayfa + nav

**Files:** Create `frontend/src/features/route-lab/RouteSimForm.tsx`, `frontend/src/pages/RouteLabPage.tsx`; Modify `App.tsx` (route), sidebar nav; Test `__tests__/RouteLabPage.test.tsx`.

- [ ] **Step 1: RouteSimForm** — mod toggle (lokasyon dropdown / manuel koord); `useLocations` (mevcut hook/service) ile lokasyon listesi; ton/arac_yasi/segment_length inputları; submit validasyon (koord modunda 4 alan zorunlu); `onSubmit(req)`. Test: toggle + validasyon + submit payload.
- [ ] **Step 2: RouteLabPage** — `usePageTitle`; RouteSimForm + useRouteSimulation; durumlar: idle/loading(spinner)/error(mesaj+422/429/502 ayrımı)/empty/success(Summary+Profile+Heatmap). coverage<100 uyarı rozeti. Test: akış (mutate→render), error, empty.
- [ ] **Step 3:** `App.tsx` `/route-lab` route (EliteLayout altında, authed); sidebar'a "Güzergah Lab" girişi (mevcut nav yapısına göre).
- [ ] **Step 4:** Run tüm route-lab testleri + `npm run lint` + `npx tsc --noEmit` (varsa) → pass.
- [ ] **Step 5:** Commit: `feat(route-lab): RouteSimForm + RouteLabPage + nav`

---

### Task 5: Gate'ler + e2e + merge

- [ ] **Step 1:** Backend: `bash -c "cd /app && python -m pytest -m 'unit or not integration' --ignore=tests/integration --ignore=app/tests/integration -q"` (route ek testleri) + ruff + mypy.
- [ ] **Step 2:** Frontend: `npx vitest --run` (tüm) + `npm run build` (vite) + `npm run lint`.
- [ ] **Step 3: e2e** — backend rebuild/docker cp; `POST /routes/simulate` (lokasyon_id ile, Mapbox 24h cache → quota-bağımsız) 200 + segments[].mid_lon dolu doğrula.
- [ ] **Step 4:** main'e ff-merge. Memory güncelle ([[route-segment-simulation]] UI ucu kapandı).

---

## Self-Review
- **Kapsam:** §3 backend→Task 1; §4 service/hook→Task 2, bileşenler→Task 3, form/sayfa/nav→Task 4; §7 test→her task + Task 5; §6 hata→Task 4 Step 2.
- **Placeholder:** Renk eşikleri (#22c55e/#f59e0b/#ef4444), projeksiyon formülü, alan adları somut. Sidebar nav konumu Task 4 Step 3'te mevcut yapıya göre (kod-doğrulama).
- **Tip tutarlılığı:** `SegmentSim` (FE) ↔ `SegmentSimResponse` (BE) alanları birebir; `colorForConsumption` Task 3'te tanımlı+test.
- **YAGNI:** harita tile yok; SVG+recharts mevcut dep.
